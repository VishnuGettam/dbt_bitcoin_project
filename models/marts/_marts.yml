version: 2

models:
    - name: bitcoin
      tests:
        - dbt_utils.equal_rowcount:
            compare_model: ref('bitcoin_stg')
      columns:
        - name: hash_key
          description: 'Hash key of the bitcoin block'
          tests:
            - not_null
            - unique
  
            